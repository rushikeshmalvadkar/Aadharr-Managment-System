FOR CRATE TABEL 'aadhar' query

CREATE TABLE `sms`.`aadhar` (
  `ID` VARCHAR(50) NOT NULL,
  `FIRST_NAME` VARCHAR(50) NOT NULL,
  `LAST_NAME` VARCHAR(50) NOT NULL,
  `FATHER_NAME` VARCHAR(20) NOT NULL,
  `GENDER` VARCHAR(10) NOT NULL,
  `PHONE_NO` INT NOT NULL,
  `STATUS` VARCHAR(10) NOT NULL,
  `DATE_OF_BIRTH` DATE NOT NULL,
  `CREATED_ON` DATETIME NOT NULL,
  `UPDATED_ON` DATETIME NOT NULL,
  PRIMARY KEY (`ID`));
  
  
  * FOR CREATE TABEL Address
  
  CREATE TABLE `sms`.`address` (
  `ID` INT NOT NULL AUTO_INCREMENT,
  `  HOME_NO` INT NOT NULL,
  `SOCITY` VARCHAR(80) NOT NULL,
  `PLACE` VARCHAR(45) NOT NULL,
  `PINCODE` INT NOT NULL,
  `  CITY` VARCHAR(45) NOT NULL,
  `COUNTRY` VARCHAR(45) NOT NULL,
  `CREATED_ON` DATETIME NOT NULL,
  `UPDATED_ON` DATETIME NOT NULL,
  PRIMARY KEY (`ID`));
  
  *FOR ADD forignkey in aadhar table
  
  ALTER TABLE `sms`.`aadhar` 
ADD COLUMN `ADDRESS_ID` INT NOT NULL AFTER `UPDATED_ON`,
ADD INDEX `ID_idx` (`ADDRESS_ID` ASC) VISIBLE;
;
ALTER TABLE `sms`.`aadhar` 
ADD CONSTRAINT `ID`
  FOREIGN KEY (`ADDRESS_ID`)
  REFERENCES `sms`.`address` (`ID`)
  ON DELETE NO ACTION
  ON UPDATE NO ACTION;
  
  
